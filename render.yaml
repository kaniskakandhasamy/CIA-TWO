services:
  - type: web
    name: book-shelf-crud
    env: node
    plan: free
    buildCommand: npm run render-build
    startCommand: npm start
    envVars:
      - key: MONGODB_URI
        sync: false
      - key: SESSION_SECRET
        sync: false
      - key: MONGODB_DB
        value: beginner_books_crud
